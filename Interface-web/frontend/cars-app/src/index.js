import React from 'react';
import ReactDOM from 'react-dom';
import Root from './Root'; // Assurez-vous que le chemin d'importation est correct

ReactDOM.render(
  <React.StrictMode>
    <Root />
  </React.StrictMode>,
  document.getElementById('root')
);
